# 塞菲莉亚 QoL — 更新记录

## 1.46.4

- 更新后重启时，新增提示：从标题画面进入游戏后才会应用新版本。

## 1.46.3

- 修复了更新后旧版本文件残留在模组文件夹中的问题。

## 1.46.2

- 修改了更新提示的文字与按钮：[立即更新・稍后提醒・跳过此更新]
- 选择[立即更新]后，下载完成会自动重启游戏。
- 局内使用 `/qol update` 获取时不会重启。
- 在 QoL 设置窗口最上方新增了[打开更新记录]。

## 1.46.1

- 更新提示在启动游戏后立刻出现。（此前需等待20秒）
- 游戏自身的启动提示（光敏性・隐私・语言・云存档）打开期间会等待。

## 1.46.0

- 有新版本时会在游戏内提示，并可直接获取。
- 获取的版本将在下次启动游戏时生效。
- 默认为「询问后安装」。可在[检查新版本]中关闭，或改为「不询问直接安装」。
- 在聊天中输入 `/qol update` 可手动检查。

## 1.45.3

- 奥德纳爆炸范围显示改为QoL「开启」专用设置，由房主在开局前决定，局内不可更改。
- 房主与访客均需 1.45.3 或更高版本才能遵循相同规则。
- 预警颜色仍为个人设置。

## 1.45.2

- 移除了图鉴禁用在每次升级和切换楼层时重复检查全部待选奖励的处理。

## 1.45.1

- 奥德纳预警跟随炸弹的实际位置，包括落地后的滑动。

## 1.45.0

- 修复了仅允许骰子作弊但未使用时，`/gd` 和 `/givedice` 仍被阻止的问题。
- 在难度缓和中新增[显示奥德纳的爆炸范围]。（默认关闭）

## 1.43.0

- 即使尚未捡到「神秘」神器，也会提前标出哪些格子会翻倍。（默认关闭）

## 1.42.0

- 历史记录中的背包可以滚动。
- 游戏内可用 PgUp/PgDn 逐行移动背包。
- 可为每类奖励单独限制重抽次数。
- 生成数、生命与伤害倍率可直接输入至10000倍。
- 会绘制通往其他玩家标记的连线。
- 删除了村庄的天赋镜。
- 仍有物品的模组附加格子不再被回收。

## 1.41.0

- 即使开启[长按持续冲刺]，非战斗时也会恢复游戏原本的奔跑。

## 1.40.2

- 修复了[友方同盟设置]连献血蝙蝠一起移除、导致献血事件消失的问题。

## 1.40.1

- 修复了队伍状态面板直接显示颜色和行距标签的问题。

## 1.40.0

- 敌人设置新增[同时出场上限不受生成倍率影响]。（默认关闭）
- 进程设置新增[立即获得经验与叶子]。（默认关闭）
- 进程设置新增[分散掉落生成与后处理]。（默认关闭）

## 1.38.0

- 按键设置行的操作与游戏设置的「键盘」页完全一致。
